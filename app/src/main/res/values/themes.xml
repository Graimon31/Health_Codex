<resources>
    <!-- Base application theme relying on device default DayNight without an action bar. -->
    <style name="Theme.HealthCodex" parent="@android:style/Theme.DeviceDefault.DayNight">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
